import * as botConfig from './bot-config.json';

const RPC_ENDPOINT = botConfig.RPC_ENDPOINT;
const WEB_URL = botConfig.WEB_URL;
const TIPLSK = botConfig.TIPLSK;

export { botConfig, RPC_ENDPOINT, WEB_URL, TIPLSK }